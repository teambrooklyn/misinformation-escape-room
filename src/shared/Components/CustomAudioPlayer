import React, { useState } from "react";
import Lightbox from "react-image-lightbox";


import { Button, Grid } from "@material-ui/core";

import "react-image-lightbox/style.css"; // This only needs to be imported once in your app
import 'react-h5-audio-player/lib/styles.css';

const CustomAudioPlayer = (props) => {
  const [isOpen, setIsOpen] = useState(false);
  const public_url = process.env.PUBLIC_URL;

  return (
    <Grid container direction="column" alignItems="center" justifyContent="center"
        style={{width: "300px"}}>
            <Grid container direction="column" alignItems="center" justifyContent="center" className={classes.voicemailPopup}>
            <Grid container direction="column" justifyContent="flex-start" alignItems="flex-start" className={classes.voicemailContainer}>
                <Typography component="p" variant="body1" className={classes.phoneNumberText}>
                +403 (340) 143-1213
                </Typography>
                <Typography component="p" variant="body1" className={classes.locationText}>
                Paris idk
                </Typography>
                <Typography component="p" variant="body1" className={classes.dayText}>
                Yesterday
                </Typography>
                <Typography component="p" variant="body1" className={classes.timeText}>
                00:38
                </Typography>
                <img src={public_url + "/images/puzzle5/info.png"} className={classes.infoIcon}/>
            </Grid>
            <AudioPlayer
                autoPlay
                src={"/images/puzzle5/voicemails/voicemail_1" + ".mp3"}
                onPlay={e => console.log("onPlay")}
            />
            <Typography component="p" variant="body1" className={classes.transcriptionText}>
                “This is an automated message that tells Molina about her Olympic times for her interviews.”
            </Typography>
            </Grid>
        </Grid>
    </Grid>


    /*<Grid item xs={12} sm={6} md={3}>
      <Button
        variant="contained"
        fullWidth
        size="large"
        style={{color: "white", backgroundColor: "#0085c7", width: "100px", marginTop:"20px"}}
        onClick={() => setIsOpen(true)}
      >
        Part {props.part + 1}
      </Button>
      {isOpen && (
        <Lightbox
          mainSrc={public_url + props.src}
          onCloseRequest={() => setIsOpen(false)}
        />
      )}
    </Grid>*/
  );
};

export default CustomLightbox;
